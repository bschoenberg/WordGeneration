package FindWordStrings;

public class Dictionary implements IDictionary {

    public Boolean isEnglishWord(String word) {
        // implementation intentionally left undefined since it wasn't being used
        return true;
    }
}

